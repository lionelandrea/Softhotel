<form method="post" action="{{ path('admin_client_delete', {'id': client.id}) }}" onsubmit="return confirm('Supprimer ce client ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client.id) }}">
    <button class="btn">Supprimer</button>
</form>